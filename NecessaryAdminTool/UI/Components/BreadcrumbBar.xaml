<UserControl x:Class="NecessaryAdminTool.UI.Components.BreadcrumbBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- TAG: #AUTO_UPDATE_UI_ENGINE #BREADCRUMB #FLEET_NAVIGATION -->
    <Border Background="#FF1A1A1A" Padding="8,4" CornerRadius="4" Margin="0,0,0,8">
        <ItemsControl x:Name="BreadcrumbItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Separator}" FontSize="10" Foreground="#FF555555"
                                   VerticalAlignment="Center" Margin="4,0"
                                   Visibility="{Binding SeparatorVisibility}"/>
                        <TextBlock Text="{Binding Label}" FontSize="10" Foreground="{Binding Foreground}"
                                   FontWeight="{Binding FontWeight}"
                                   VerticalAlignment="Center" Cursor="Hand"
                                   MouseLeftButtonDown="Segment_Click" Tag="{Binding}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
