<UserControl x:Class="NecessaryAdminTool.ADObjectBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource BgDarkestBrush}">

    <!-- TAG: #AD_OBJECT_BROWSER #RSAT_ADUC #VERSION_7 -->
    <!-- Replicates RSAT Active Directory Users and Computers structure -->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: Tree View (like ADUC left pane) -->
        <Border Grid.Column="0" Background="{DynamicResource BgDarkBrush}" BorderBrush="{DynamicResource BorderDimBrush}" BorderThickness="0,0,1,0">
            <DockPanel>
                <!-- Header -->
                <Border DockPanel.Dock="Top" Background="{DynamicResource BgMediumBrush}" Padding="12,10" BorderBrush="{DynamicResource AccentOrangeBrush}" BorderThickness="0,0,0,2">
                    <StackPanel>
                        <TextBlock Text="🌳 ACTIVE DIRECTORY" Foreground="{DynamicResource AccentOrangeBrush}" FontSize="11" FontWeight="Bold"/>
                        <TextBlock x:Name="TxtDomainName" Text="Loading..." Foreground="{DynamicResource AccentZincBrush}" FontSize="9" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Tree View -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
                    <TreeView x:Name="ADTreeView" Background="Transparent" BorderThickness="0"
                              SelectedItemChanged="ADTreeView_SelectedItemChanged">
                        <TreeView.Resources>
                            <!-- TreeViewItem Style -->
                            <Style TargetType="TreeViewItem">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="Padding" Value="4,3"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TreeViewItem">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition/>
                                                </Grid.RowDefinitions>

                                                <!-- Header -->
                                                <Grid Grid.Row="0">
                                                    <!-- Overlays (behind content; opacity toggled by triggers) -->
                                                    <Border x:Name="hoverOverlay" CornerRadius="3" Margin="0,1" Opacity="0"
                                                            Background="{DynamicResource BgLightBrush}"/>
                                                    <Border x:Name="selectionOverlay" CornerRadius="3" Margin="0,1" Opacity="0"
                                                            Background="{DynamicResource AccentGradientBrush}"/>
                                                    <!-- Content border (transparent — always on top of overlays) -->
                                                    <Border x:Name="Bd" Background="Transparent"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            Padding="{TemplateBinding Padding}"
                                                            CornerRadius="3" Margin="0,1">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>

                                                            <!-- Expander Toggle -->
                                                            <ToggleButton x:Name="Expander" Grid.Column="0"
                                                                          IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                                          ClickMode="Press" Width="16" Height="16"
                                                                          Background="Transparent" BorderThickness="0"
                                                                          Margin="0,0,4,0">
                                                                <ToggleButton.Template>
                                                                    <ControlTemplate TargetType="ToggleButton">
                                                                        <Border Background="Transparent" Width="16" Height="16">
                                                                            <TextBlock x:Name="ExpanderIcon" Text="▶"
                                                                                       Foreground="{DynamicResource AccentZincBrush}" FontSize="8"
                                                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsChecked" Value="True">
                                                                                <Setter TargetName="ExpanderIcon" Property="Text" Value="▼"/>
                                                                                <Setter TargetName="ExpanderIcon" Property="Foreground" Value="#FFFF8533"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </ToggleButton.Template>
                                                            </ToggleButton>

                                                            <!-- Content -->
                                                            <ContentPresenter x:Name="PART_Header" Grid.Column="1"
                                                                              ContentSource="Header" VerticalAlignment="Center"
                                                                              HorizontalAlignment="Left"/>
                                                        </Grid>
                                                    </Border>
                                                </Grid>

                                                <!-- Children -->
                                                <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Margin="16,0,0,0"/>
                                            </Grid>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsExpanded" Value="False">
                                                    <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                                </Trigger>
                                                <Trigger Property="HasItems" Value="False">
                                                    <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="selectionOverlay" Property="Opacity" Value="1"/>
                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="hoverOverlay" Property="Opacity" Value="1"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TreeView.Resources>

                        <!-- Root node will be populated dynamically -->
                        <TreeViewItem Header="🌐 Loading domain..." IsExpanded="True"/>
                    </TreeView>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- SPLITTER -->
        <GridSplitter Grid.Column="1" Width="5" Background="{DynamicResource BorderDimBrush}" HorizontalAlignment="Stretch"/>

        <!-- RIGHT PANEL: Object List and Details (like ADUC right pane) -->
        <Border Grid.Column="2" Background="{DynamicResource BgDarkBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" Background="{DynamicResource BgMediumBrush}" Padding="12,8" BorderBrush="{DynamicResource BorderDimBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock x:Name="TxtSelectedContainer" Text="No container selected"
                                       Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtObjectCount" Text="(0 objects)"
                                       Foreground="{DynamicResource AccentZincBrush}" FontSize="10" Margin="8,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="BtnScanSelected" Content="🔍 SCAN SELECTED"
                                    Click="BtnScanSelected_Click"
                                    Padding="10,6" FontSize="10" FontWeight="SemiBold"
                                    Margin="0,0,6,0">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" CornerRadius="4" Padding="{TemplateBinding Padding}"
                                                Background="{DynamicResource AccentGradientBrush}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="BtnRefresh" Content="🔄 REFRESH"
                                    Click="BtnRefresh_Click"
                                    Background="{DynamicResource BgLightBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                                    BorderBrush="{DynamicResource BorderBrightBrush}" BorderThickness="1"
                                    Padding="10,6" FontSize="10" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Search and Filter bar -->
                <Border Grid.Row="1" Background="{DynamicResource BgMediumBrush}" Padding="8,5" BorderBrush="{DynamicResource BorderDimBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search box -->
                        <Border Grid.Column="0" Background="{DynamicResource BgDarkBrush}" BorderBrush="{DynamicResource BorderDimBrush}" BorderThickness="1"
                                CornerRadius="4" Margin="0,0,6,0">
                            <Grid>
                                <TextBlock x:Name="SearchPlaceholder" Text="🔍  Search by name, account, or info..."
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="10"
                                           VerticalAlignment="Center" Margin="8,0" IsHitTestVisible="False"/>
                                <TextBox x:Name="TxtSearch" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                                         BorderThickness="0" FontSize="10" Padding="8,5"
                                         TextChanged="TxtSearch_TextChanged" CaretBrush="{DynamicResource TextPrimaryBrush}"/>
                            </Grid>
                        </Border>

                        <!-- Type filter dropdown -->
                        <ComboBox x:Name="CboTypeFilter" Grid.Column="1" Width="115"
                                  SelectionChanged="CboTypeFilter_SelectionChanged"
                                  Foreground="{DynamicResource TextPrimaryBrush}" FontSize="10">
                            <ComboBoxItem Content="All Types" IsSelected="True"/>
                            <ComboBoxItem Content="🖥 Computers"/>
                            <ComboBoxItem Content="👤 Users"/>
                            <ComboBoxItem Content="👥 Groups"/>
                            <ComboBoxItem Content="📁 OUs"/>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- Object List -->
                <DataGrid x:Name="GridADObjects" Grid.Row="2"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          Background="{DynamicResource BgDarkBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{DynamicResource BgLightBrush}"
                          BorderThickness="0" HeadersVisibility="Column"
                          SelectionMode="Extended" SelectionUnit="FullRow">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource BgLightBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentOrangeBrush}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="10"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderDimBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{DynamicResource BgDarkBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF252525"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentGradientBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="10,6"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="  " Binding="{Binding Icon}" Width="40" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" MinWidth="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding ObjectType}" Width="90"/>
                        <DataGridTextColumn Header="Account / DNS" Binding="{Binding Account}" Width="180"/>
                        <DataGridTextColumn Header="OS / Email / Info" Binding="{Binding Info}" Width="3*" MinWidth="160"/>
                        <!-- Enabled status — green for active, red for disabled, blank for OUs -->
                        <DataGridTemplateColumn Header="Status" Width="95">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding EnabledDisplay}" FontSize="10"
                                               VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource AccentGreenBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDisabled}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ObjectType}" Value="OU">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ObjectType}" Value="Other">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ObjectType}" Value="Group">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Status Bar -->
                <Border Grid.Row="3" Background="{DynamicResource BgMediumBrush}" Padding="12,6" BorderBrush="{DynamicResource BorderDimBrush}" BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="TxtStatusMessage" Grid.Column="0"
                                   Text="Ready" Foreground="{DynamicResource AccentZincBrush}" FontSize="9"/>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Computers: " Foreground="{DynamicResource AccentZincBrush}" FontSize="9"/>
                            <TextBlock x:Name="TxtComputerCount" Text="0" Foreground="{DynamicResource AccentOrangeBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,12,0"/>

                            <TextBlock Text="Users: " Foreground="{DynamicResource AccentZincBrush}" FontSize="9"/>
                            <TextBlock x:Name="TxtUserCount" Text="0" Foreground="{DynamicResource AccentOrangeBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,12,0"/>

                            <TextBlock Text="Groups: " Foreground="{DynamicResource AccentZincBrush}" FontSize="9"/>
                            <TextBlock x:Name="TxtGroupCount" Text="0" Foreground="{DynamicResource AccentOrangeBrush}" FontSize="9" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
